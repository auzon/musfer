apiVersion: apps/v1
kind: Deployment
metadata:
  name: transfer-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: transfer-service
  template:
    metadata:
      labels:
        app: transfer-service
    spec:
      containers:
        - name: transfer-service
          image: transfer-service:local
          resources:
            requests:
              cpu: 250m
              memory: 128Mi
              ephemeral-storage: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
              ephemeral-storage: 256Mi
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
          - name: DB_HOST
            valueFrom:
              secretKeyRef:
                name: transfer-service-secrets
                key: DB_HOST

          - name: DB_PORT
            valueFrom:
              secretKeyRef:
                name: transfer-service-secrets
                key: DB_PORT

          - name: DB_NAME
            valueFrom:
              secretKeyRef:
                name: transfer-service-secrets
                key: DB_NAME

          - name: DB_USER
            valueFrom:
              secretKeyRef:
                name: transfer-service-secrets
                key: DB_USER

          - name: DB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: transfer-service-secrets
                key: DB_PASSWORD
      automountServiceAccountToken: false
---
apiVersion: v1
kind: Service
metadata:
  name: transfer-service
spec:
  selector:
    app: transfer-service
  ports:
    - port: 80
      targetPort: 8080

